import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const group_permissions = () => {
  return (
    <View>
      <Text>group_permissions</Text>
    </View>
  );
};

export default group_permissions;

const styles = StyleSheet.create({});

import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const pick_upScreen = () => {
  return (
    <View>
      <Text>pick_upScreen</Text>
    </View>
  );
};

export default pick_upScreen;

const styles = StyleSheet.create({});
